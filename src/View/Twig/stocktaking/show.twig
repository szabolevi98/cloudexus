{% extends 'common/layout.twig' %}

{% block content %}
    <div class="cx-card">
        <div class="cx-card__header">
            <div>
                <h2>{{ stocktaking.stocktaking_number }}</h2>
                <div class="text-muted small">
                    {{ stocktaking.warehouse_name }} · {{ stocktaking.created_at }}
                    {% if stocktaking.created_by_name %} · {{ stocktaking.created_by_name }}{% endif %}
                    {% if stocktaking.note %} · {{ stocktaking.note }}{% endif %}
                </div>
            </div>
            <div>
                {% if stocktaking.diff_count > 0 %}
                    <span class="badge rounded-pill text-bg-warning-subtle text-warning-emphasis">{{ t('stocktaking.variance_corrected', {'count': stocktaking.diff_count}) }}</span>
                {% else %}
                    <span class="badge rounded-pill text-bg-success-subtle text-success-emphasis">{{ t('stocktaking.no_variance') }}</span>
                {% endif %}
            </div>
        </div>
        <div class="cx-card__body p-0">
            <div class="table-responsive">
                <table class="table cx-table align-middle mb-0">
                    <thead>
                    <tr>
                        <th>{{ t('common.sku') }}</th>
                        <th>{{ t('stocktaking.col_name') }}</th>
                        <th class="text-end">{{ t('stocktaking.col_book_quantity') }}</th>
                        <th class="text-end">{{ t('stocktaking.col_counted_quantity') }}</th>
                        <th class="text-end">{{ t('stocktaking.col_variance') }}</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in stocktaking.items %}
                        <tr>
                            <td class="fw-medium">{{ item.sku }}</td>
                            <td>{{ item.product_name }}</td>
                            <td class="text-end text-muted">{{ item.book_quantity|qty }} {{ item.unit }}</td>
                            <td class="text-end">{{ item.counted_quantity|qty }} {{ item.unit }}</td>
                            <td class="text-end fw-medium {{ item.diff == 0 ? 'text-muted' : (item.diff > 0 ? 'text-success' : 'text-danger') }}">
                                {{ item.diff > 0 ? '+' : '' }}{{ item.diff|qty }}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <a href="{{ base_url }}/stocktaking" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>{{ t('common.back_to_list') }}
    </a>
{% endblock %}
